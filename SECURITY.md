# Security

LOTECH Photo Metadata Editor is a local-first macOS application.

It requests access only to folders selected by the user and uses macOS security-scoped access for file operations. It should not upload photo metadata or image files to remote services as part of normal operation.

Report security issues privately to the project maintainer.
